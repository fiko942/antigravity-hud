# Git Commit & Push Rule

Whenever completing features, fixes, or documentation tasks:
1. Always stage and commit changes locally with clear conventional messages (e.g. `feat: ...`, `fix: ...`, `docs: ...`).
2. **NEVER run `git push`** or attempt to push to remote git remotes. The user manages pushing manually.
